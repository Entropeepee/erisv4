# ═══════════════════════════════════════════════════════════
# Eris Echo v4 — Dependencies
# ═══════════════════════════════════════════════════════════

# CORE (runs everywhere, no GPU needed)
numpy>=1.24

# SERVER (for web UI + API)
fastapi>=0.104
uvicorn>=0.24
# websockets >=12 fixes the legacy-protocol keepalive assertion bug
# (websockets/legacy/protocol.py _drain_helper) that dropped the /ws connection.
websockets>=12.0
httpx>=0.25
psutil>=5.9          # cockpit system vitals (CPU/RAM)
# wmi>=1.5            # (Windows only) CPU temperature via LibreHardwareMonitor

# GPU ACCELERATION (RTX 5080 / CUDA 13.2)
# Uncomment when CUDA toolkit is installed:
# cupy-cuda13x>=14.0

# EMBEDDING MODEL (for semantic memory retrieval)
# Uncomment when ready to add embeddings:
# onnxruntime-gpu>=1.17
# sentence-transformers>=2.3

# VECTOR INDEX — scales semantic search for the document library / ChatGPT
# history. CPU build installs cleanly via pip and is fast for <1M vectors;
# LongTermMemory uses it automatically when present, else brute-force.
faiss-cpu>=1.8
# Optional GPU upgrade (needs CUDA/conda): faiss-gpu-cuvs>=1.14

# DOCUMENT LIBRARY (browser upload + ErisLibrary folder ingestion)
# .txt/.md/.json read with the standard library; PDF/DOCX need these:
PyMuPDF>=1.24        # PDF text extraction (import fitz)
python-docx>=1.1     # .docx read + authoring (.docx export)
python-multipart>=0.0.9  # browser file uploads (FastAPI UploadFile)
reportlab>=4.0       # PDF authoring (document export)

# TESTING
pytest>=8.0

# ── Remediation v4.1 additions ──────────────────────────────
# Knowledge / grounding stack. The local instance runs WITHOUT any of these:
#   * web_search & web_reader use only the Python standard library (urllib).
#   * embeddings default to a fast deterministic fallback (no model needed).
#   * ask_expert is dormant until ANTHROPIC_API_KEY is set.
# Install these only to UPGRADE a capability:
#
# Real semantic embeddings (set ERIS_EMBEDDINGS=on to activate):
# sentence-transformers>=2.3
# External research oracle (set ANTHROPIC_API_KEY to activate):
# anthropic>=0.39
